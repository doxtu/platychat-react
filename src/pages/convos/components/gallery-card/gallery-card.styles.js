import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
  root: {
    padding: '20px',
    maxHeight: '480px',
    overflowY: 'auto',
  },
  img: {
    width: '100%',
  },
})
